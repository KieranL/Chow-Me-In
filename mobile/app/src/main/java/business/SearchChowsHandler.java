package business;

import java.util.ArrayList;

public class SearchChowsHandler {

    public static ArrayList<String> searchForChow(CharSequence query) {
        final ArrayList<String> list = new ArrayList<>();
        list.add("Nice");
        list.add("yah");
        list.add("sweet");
        /*
        for (int i = 0; i < values.length; ++i) {
            list.add(values[i]);
        }*/
        return list;
    }
}
